# Port
PORT=5000

# CORS
CORS_ORIGINS=http://localhost:3000,https://edufutura.com

# إعدادات قاعدة البيانات - MongoDB Atlas
MONGODB_URI=mongodb+srv://AD:Admin%40123456789@cluster0.rzwihci.mongodb.net/edufutura?retryWrites=true&w=majority&appName=Cluster0

# إعدادات المصادقة
JWT_SECRET=EduFutura-JWT-Secret-987654321
JWT_EXPIRES_IN=7d

# Admin
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123

# Redis (معطل)
ENABLE_REDIS=false

# Clustering (معطل)
USE_CLUSTERING=false

NODE_ENV=development
