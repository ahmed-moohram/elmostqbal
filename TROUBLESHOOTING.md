# 🔧 دليل حل المشاكل الشائعة

## ✅ **تم حل المشكلة: ChunkLoadError**

### المشكلة:
```
ChunkLoadError: Loading chunk app/courses/page failed
```

### الحل:
1. **إيقاف الخادم:** `Ctrl+C`
2. **حذف مجلد البناء:**
   ```powershell
   Remove-Item -Recurse -Force .next
   ```
3. **إعادة تشغيل الخادم:**
   ```bash
   npm run dev
   ```

---

## 🔐 **النظام يعمل الآن بالتخزين المحلي**

### الحسابات الجاهزة:

#### **حساب الأدمن:**
- **الهاتف:** `01005209667`
- **كلمة المرور:** `Ahmed@010052`
- **يوجه إلى:** `/admin/dashboard`

#### **أي حساب جديد:**
1. سجل من: http://localhost:3000/register
2. استخدم نفس البيانات للدخول

---

## 📝 **مشاكل أخرى وحلولها:**

### 1. **Invalid API Key**
**الحل:** النظام يعمل بالتخزين المحلي، لا حاجة لـ API key

### 2. **لا يمكن الوصول إلى الخدمة**
**الحل:** النظام يعمل محلياً بدون إنترنت

### 3. **صفحة بيضاء أو خطأ 404**
**الحل:**
```bash
# أوقف الخادم
Ctrl+C

# احذف الملفات المؤقتة
Remove-Item -Recurse -Force .next
Remove-Item -Recurse -Force node_modules/.cache

# أعد التشغيل
npm run dev
```

### 4. **Module not found**
**الحل:**
```bash
npm install
```

### 5. **Port 3000 is already in use**
**الحل:**
```powershell
# اقتل العمليات
taskkill /F /IM node.exe

# أو استخدم منفذ آخر
npm run dev -- -p 3001
```

---

## 🚀 **الأوامر المفيدة:**

### تشغيل المشروع:
```bash
cd D:\2\معتصم\frontend
npm run dev
```

### فتح المتصفح:
```
http://localhost:3000
```

### مسح البيانات المحلية (Console):
```javascript
localStorage.clear()
```

### عرض المستخدمين المسجلين (Console):
```javascript
JSON.parse(localStorage.getItem('localUsers') || '[]')
```

---

## ✨ **الصفحات الرئيسية:**

- **الرئيسية:** http://localhost:3000
- **الدورات:** http://localhost:3000/courses
- **تسجيل الدخول:** http://localhost:3000/login
- **التسجيل:** http://localhost:3000/register
- **لوحة التحكم:** http://localhost:3000/dashboard
- **لوحة الأدمن:** http://localhost:3000/admin/dashboard

---

## 📌 **ملاحظات مهمة:**

1. **النظام يعمل محلياً** - لا يحتاج إنترنت
2. **البيانات في المتصفح** - لن تنتقل لجهاز آخر
3. **التخزين المحلي آمن** - للتجربة والتطوير
4. **للإنتاج:** احصل على API key صحيح من Supabase

---

## 🎯 **النتيجة:**

**النظام يعمل بشكل كامل الآن! 🎉**
- ✅ التسجيل يعمل
- ✅ تسجيل الدخول يعمل
- ✅ الصفحات تعمل
- ✅ لا أخطاء
