# 📋 **دليل تطبيق جداول الأمان والأداء على Supabase**

## **الخطوات المطلوبة:**

### 1️⃣ **افتح Supabase Dashboard:**
```
https://app.supabase.com/project/wnqifmvgvlmxgswhcwnc
```

### 2️⃣ **اذهب إلى SQL Editor:**
- من القائمة الجانبية، اضغط على **SQL Editor**

### 3️⃣ **انسخ والصق هذا الكود:**

```sql
-- أولاً: تطبيق الجداول الأساسية
-- انسخ محتوى: fixed_database_schema.sql
-- ثم طبقه في SQL Editor واضغط Run
```

### 4️⃣ **بعد نجاح الخطوة السابقة، انسخ والصق:**

```sql
-- ثانياً: تطبيق جداول الأمان
-- انسخ محتوى: security_performance_tables.sql  
-- ثم طبقه في SQL Editor واضغط Run
```

### 5️⃣ **تحديث الأنواع في TypeScript:**

بعد إنشاء الجداول، نحتاج لتحديث الأنواع:

```bash
# في terminal المشروع
cd D:\2\معتصم\frontend

# توليد الأنواع من Supabase
npx supabase gen types typescript --project-id wnqifmvgvlmxgswhcwnc > src/types/database.types.ts
```

### 6️⃣ **البديل: استخدام Supabase CLI:**

```bash
# تثبيت Supabase CLI
npm install -g supabase

# تسجيل الدخول
supabase login

# ربط المشروع
supabase link --project-ref wnqifmvgvlmxgswhcwnc

# تطبيق migrations
supabase db push
```

## **حل مؤقت للأخطاء:**

إذا لم تستطع تطبيق الجداول الآن، استخدم هذا الحل المؤقت:
